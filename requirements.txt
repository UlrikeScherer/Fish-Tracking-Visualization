# Runtime dependencies — pinned ranges match [project].dependencies in pyproject.toml.
# Build-time pins (Cython==0.29.34, setuptools==65.6.3) live in
# [build-system].requires in pyproject.toml and are NOT needed here.
matplotlib~=3.9.0
numpy==1.24.2
pandas~=2.2.2
plotly==5.0.0
scipy==1.10.1
seaborn==0.11.1
tqdm>=4.66.3
python-dotenv~=1.2.1
